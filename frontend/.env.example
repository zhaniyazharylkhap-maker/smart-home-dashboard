# FastAPI base URL (browser uses this — must match where port 8000 is reachable).
# Default in code: http://127.0.0.1:8000
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Optional WebSocket base; empty = ws://<page hostname>:8000
NEXT_PUBLIC_WS_URL=
